"""用户账号 / 会话认证子模块。

提供：注册 / 登录 / 登出 / 当前用户接口，服务端会话（Cookie + 数据库会话表），
以及「探索页登录门禁」中间件与审计用户身份解析。密码使用 bcrypt 加盐哈希。

设计要点：
- 与 :mod:`api.audit` 对称，同样提供**同步 + 异步**两套存储接口（``UserStore`` /
  ``SyncUserStore``），共享同一套 ORM 模型，便于中间件（异步）与脚本（同步）复用。
- 会话 Cookie 为 opaque 随机串（``HttpOnly`` + ``SameSite=Lax``），会话记录存数据库，
  服务端可主动吊销 / 过期，天然支持「已登录用户身份」写入审计。
- 不依赖任何签名密钥：Cookie 只持有随机会话 id，抗伪造靠数据库校验。
"""
